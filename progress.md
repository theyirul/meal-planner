---
title: 매일아침 (MealMorning) — 진행 상황
created: 2026-06-04
updated: 2026-08-03
status: confirmed
category: personal
---

> 제품 개요·빌드 구조·운영 규칙은 같은 폴더의 CLAUDE.md 참조. 이 파일은 진행 상황만.

## 현재 상태
- 단계: 8월 운영 중. **3곳(동대문·광진·창원) 8월 반영 완료.**
- 다음 액션: 앱에서 광진·창원 8/31 열어 원본 식단표 첫 행과 대조 (고아 월요일 확정 검증).
- 백로그(제품): `webapp-prd`로 B안 PRD 작성 (미착수, 급하지 않음).
- 블로커: 없음

## 방향 메모 (2026-06-04 합의)
- 제품 전환: "운영자(룰)가 매달 손으로 자료 받아 적용" → **부모가 자기 애 식단 올려서 자기가 쓰는 개인 도구(B안)**.
- A안(공개 검증 디렉토리)은 검증 책임·서버 부담·안전 리스크 때문에 보류.
- B안 핵심: 아무 형식 PDF·이미지 업로드 → AI가 메뉴·알레르기 추출 → "⚠️ 확인하세요" 본인 검수 → 알레르기 필터 적용·기기 저장. 포맷별 파서·운영자·빌드/푸시 사이클 전부 불필요해짐.

## B안 v1 설계 합의 (2026-07-11, grilling 세션)
룰이 겁내던 두 공포(업로드 서버 악용·OCR 비용)를 v1 범위 축소로 소멸시킴. 다음은 이 내용을 `webapp-prd`로 PRD화.
1. **무엇** — 여러 부모용 제품, 룰이 퍼소나이자 첫 사용자. 단계적(룰·서클 먼저 → 대중 나중).
2. **아키텍처** — 무회원, 무업로드서버. 데이터는 각 기기 localStorage.
3. **형식(v1)** — PDF + CSV만. HWP는 *사용자가 PDF로 저장해* 올림(온보딩 문구). 네이티브 HWP·이미지는 phase 2.
4. **파싱** — 브라우저 텍스트 추출(pdf.js, 무비용) → **AI 텍스트 호출로 아무 형식이나 구조화**(B안의 영혼, 기관별 파서 폐기). 비전 아님.
5. **자금** — 룰 키를 쥔 얇은 프록시 + **월 하드 상한**. BYOK 아님. 텍스트라 비용 거의 0.
6. **안전** — 형식별 신뢰도 차등. v1(텍스트)은 가벼운 검수(원본 나란히 + AI ⚠️ 자기표시). 책임은 부모(면책)이되 검수는 쉽게. 이미지 무거운 검수는 phase 2.
7. **사용자(초반)** — 아는 사람들끼리(비공개). **지금 룰에게 파일 건네던 실사용자들을 그대로 셀프 업로드로 이주.** 캡차·IP제한은 공개할 phase 2.
8. **기존 3곳** — (c) 동결. 과거 데이터 안 지움. **룰의 매달 손 노동만 종료**(8월 재개 대신 셀프 업로드로 대체). ← 위 "다음 액션"의 8월 재개를 이 방향으로 대체 검토.
- ※ **2026-08-02 현실**: B안 미착수 상태라 8월도 수동 운영함(동대문·광진 완료). 셀프 업로드 이주는 v1이 나온 뒤로 미뤄짐.
- **Phase 2(v1 검증 후):** 이미지 비전 파싱 + 네이티브 HWP(서버) + 공개 확장(프록시 캡차·상한 강화) + 대중 부모.
- **PRD에서 채울 것:** 프록시 위치(Cloudflare Worker 등)·월 상한 금액 / 부모 알레르기 프로필 설정 UX(기존 필터 로직 재사용) / 핵심 복사 플로우(오늘 식단→자동체크→복사→메신저) 유지.

## 백로그
- 커스텀 도메인 연결 (URL 외우기 어려움) — `매일아침.kr` 등 + GitHub Pages CNAME. *현 정적 사이트 기준 빠른 개선이지만, B안으로 가면 도메인 정책 재검토.*
- ~~동대문구 가로형 정식 파서(`dongdaemun_horizontal`) detect_table_format 등록~~ **취소 (2026-08-02)** — 8월부터 표준조리법 PDF 단독 파싱(`parse_dongdaemun_recipe.py`)으로 전환해 가로형 식단표 파서 자체가 불필요해짐.
- **날짜 없는 특별식 표시 방법** (동대문 `[생일식단]` 9항목 + 창원 테마식단 표: 생일·견학·행사·방학·김장데이). 두 지역 모두 원본에 실려 있는데 현재 전부 제외 중. 날짜 기반 UI라 붙일 자리가 없음 — 별도 탭이나 "이달의 특별식" 영역이 필요. *B안으로 가면 재검토.*

## 최근 로그
### 2026-08-03
- **버그 수정: 월간 뷰 날짜가 한 칸씩 밀림** — 8/3(월)이 금요일 칸에 표시. `buildCalendar`가 앞 빈 칸을 **1일 기준**으로 세는데, 토요일을 숨기면 그리드가 5열(월~금)이 되고 1일(토)은 사라진다. 그 보정을 `Math.min(emptyCells,4)`로 얼버무려 첫 표시일이 한 칸 밀렸다. **'실제로 표시되는 첫 날' 기준**으로 세도록 변경.
  - 발현 조건: **토요일 숨기기 ON + 1일이 토요일인 달**. 2026·2027년 중 해당 달은 2026-08과 2027-05뿐 — 이번 달에 처음 드러났다. 24개월 전수 시뮬레이션으로 두 모드 모두 불일치 0 확인.
- **창원 8월 반영 (HWP 입력)** — 24일자 182항목. 조리지시서 음식명 총합 182와 정확히 일치(누락·중복 0). 레시피 매칭 130/182(71%). 8/15 광복절·8/17 대체공휴일 무메뉴 정상 확인. 8/31(월)은 광진과 마찬가지로 첫 행 8/1(토) 옆에 끼워진 **고아 월요일** — 요일 검증 통과.
- **창원 8월부터 식단표 포맷 변경**, `parse_hwp.py` 3가지 보강:
  1) 날짜 행 헤더가 `날짜` → `일자`로 바뀜 (`DATE_HDR`로 둘 다 허용).
  2) **알레르기 원문자만 있는 고아 토큰 15건** — 칸이 좁아져 셀 안에서 줄바꿈되며 메뉴명과 떨어짐(`생선까스&타르타르소스` + `①②⑤⑥⑫`). 기존 코드는 이름 없는 토큰을 **버렸다** → 알레르기 유실. 앞 메뉴에 병합하도록 수정. **아이 건강 직결 버그.**
  3) 줄바꿈으로 갈라진 `(고춧가루제외)`는 뒤 메뉴의 수식어로, `삼색나물비빔밥&`+`간장양념장`은 한 항목으로 결합. `복숭아⑪ 또는 창원시지원과일`(12건)은 한 항목으로 묶음.
  - 7월 파일 재파싱 결과 26일자 193항목으로 동일 → 회귀 없음. 7월 파일엔 고아 토큰 0건이라 과거 데이터도 무피해.
- **`crosscheck.py` 보강** — 창원 8월 조리지시서엔 `에너지 및 영양소 분석표` 시트가 없어 교차검증이 **조용히 스킵되고 있었음**(None 반환). 조리지시서 시트에도 날짜·끼니 열이 있어 그걸로 배정 대조를 돌리게 폴백 추가. `또는` 선택지 표기변형은 일치로 처리, 지시서에 날짜 라벨이 빈 항목은 별도 표시.
- **교차검증 결과**: 배정 대조 72블록 중 불일치 3건 — 전부 8/24(월)로, **조리지시서 5주차 시트의 `24[월]` 날짜 라벨만 비어 있는 원본 오류**(메뉴 내용은 식단표와 일치). 실질 배정 오류 0건.
- **알레르기 미표기 3건 → 표기하기로 결정(룰 지시). 원본 충실보다 안전 우선.** 조리지시서 식재료를 근거로 보강, 교차검증 누락 후보 3건 → 0건.
  - 8/8 점심 탕수육 +①(달걀) / 8/22 점심 토마토스파게티 +⑩(돼지고기_앞다리) / 8/25 점심 꼬마돈까스&소스 +①(달걀)
  - 수단: `uploads/202608_창원시_1-2세_일반형_패치.json`. **패치 JSON에 `allergy` 연산 신설** — 이름·레시피를 보존하고 번호만 병합한다(remove+add로 갈아끼우면 레시피가 날아감).
  - **패치가 HWP 입력에선 아예 무시되고 있었음** — `process_upload_group`이 HWP·수동 JSON 경로에서 조기 return해 패치 루프를 건너뛰었다. `_apply_patches`로 묶어 세 경로 모두 거치게 수정.
  - 앞으로 판단 기준: 조리지시서 식재료에 근거가 있으면 원본 미표기라도 보강한다(7월의 '원본 유지' 선례를 이 방향으로 갱신).
- 그 외 원본 특이점: 8/31 깍두기에만 ⑨ 표기가 없음(다른 날은 전부 있음). 원본 그대로 유지.

### 2026-08-02
- **동대문 8월 반영 — 식단표 없이 표준조리법 PDF만으로 빌드.** 24일자 187항목, 원본 셀 수와 정확히 일치(누락·중복 0). 레시피 매칭 160/187(85%, 7월 대비 개선). 8/15 광복절·8/17 대체공휴일·일요일 5일이 빠진 것까지 정상 확인.
- **`scripts/parse_dongdaemun_recipe.py` 정식 등록** — 동대문 표준조리법 PDF는 날짜·끼니·음식명·**알레르기 원문자**를 표에 다 담고 있어 식단표 없이 식단 복원이 가능하다. 알레르기는 원본 표기 그대로(추정 없음). 7월엔 scratchpad 임시 스크립트였던 걸 재사용 가능하게 만듦.
- 마지막 페이지 `[생일식단]`(날짜 없는 9항목)은 앞 날짜에 딸려 들어가지 않게 끊고 제외. 날짜 기반 앱이라 붙일 자리가 없음. **미해결 — 붙일 방법 필요하면 별도 논의.**
- **광진은 조리법만으로 불가**: 메뉴명·식재료명 열에 알레르기 원문자 0건(원문자 113건은 전부 조리방법 단계번호 ①②③였음). 알레르기가 식단표에만 있음 → 식단표 HWP를 따로 받아 처리.
- **광진 8월 반영 (HWP 입력)** — 24일자 182항목, 레시피 매칭 182/182(100%). 원본 메뉴 칸 156 + 슬래시 분리 22 + 대체메뉴 분리 4 = 182로 수치 일치 확인.
- **`scripts/parse_hwp_gwangjin.py` 신규** — 광진 HWP는 창원과 구조가 완전히 다르다: 헤더가 `일자`, 날짜가 `31(월)` 괄호형, **알레르기가 원문자가 아니라 `(5,6)` 아라비아 숫자**, 메뉴가 셀 안 공백 구분이 아니라 행마다 세로 적층(메뉴명에 공백 포함 가능). 병합셀 run 단위로 묶고 끼니 라벨 다수결로 경계 보정(라벨 rowspan과 메뉴 rowspan이 어긋남 — 8/31 단호박범벅 1건 발생).
- **고아 월요일 주의**: 광진 식단표는 8/31(월)을 첫 행 8/1(토) 옆에 끼워 넣는다. 7/31은 금요일이고 7월 식단표에 이미 다른 메뉴로 들어있어 8/31로 확정. 매달 첫 행 날짜를 요일로 검증할 것.
- **build.py 레시피 폴백 추가**: 광진 레시피 시트 헤더가 `음식명`이 아니라 `메뉴명`이라 `parse_recipe_xlsx`가 0개를 반환했음(7월엔 PDF 경로라 안 드러남). `_flatten_menu_recipes`로 `extract_menus` 재사용 + 레시피명이 더 긴 경우(`소고기콩나물밥&양념장`)용 별칭 등록.
- `uploads/`의 6·7월 파일은 `uploads/_처리완료_2606_2607/`로 이동(삭제 아님).
- **운영 지역 3곳으로 정리** — 창원·광진·동대문만 남기고 7곳(성남·수원·용인·대전중구·강서·영등포·옥천) 임베딩 삭제. 전부 2026-03~04에서 멈춰 있던 곳. index.html 936KB → 600KB. 삭제분은 git 히스토리(`8f71814` 이전)에 남아 있어 복구 가능. build.py의 REGION_MAP 항목은 남겨둠(파일이 다시 들어오면 그대로 동작).

### 2026-07-02
- **창원시 7월 일반형 추가 (HWP 입력)** — 26일자(17일 제헌절 휴무 제외), 193항목. changwon/2026-07=일반형(6월은 죽형).
- **HWP 정식 지원**: `scripts/parse_hwp.py`(pyhwp→HTML→병합셀 그리드 파싱) + build.py 라우팅. `.hwp` 식단표 드롭하면 자동 처리. 커밋 `4d1866b`.
- **조리지시서 교차검증 도구화**: `scripts/crosscheck.py` — 영양소분석표(요일·끼니·음식명)로 배정 대조 + 식재료→알레르기 누락 flag. 창원 7월 검증 결과 배정 오류 0건, 알레르기 3건은 식단표 원본이 미표기(추출은 정확). **앞으로 조리지시서 있으면 항상 함께 교차검증** (룰 지침).
- 그대로 푸시(원본 충실).

### 2026-07-01
- **7월 식단 2곳 추가: 동대문구·광진구** — 둘 다 1-2세 일반형, 26일자(17일 제헌절 무메뉴 정상 제외).
- 동대문구: 6월과 같은 **가로형 그리드**지만 이번엔 텍스트 레이어가 깨끗해서 **pdfplumber `extract_tables` + 괄호깊이 토크나이저로 자동 추출**(6월 좌표 OCR보다 정확). 셀 구분자는 `\n`+괄호밖 `/`, 숫자만 조각은 앞 메뉴에 병합. 룰 알레르기 검증 후 반영. 파서 스크립트는 scratchpad에 보관(정식 등록은 B안 전환 고려해 보류). 레시피 150/205 매칭. 푸시 `4646fca`.
- 광진구: 표준 PDF+xlsx 자동 파싱(검증된 경로). 처음 받은 파일이 3-5세라 1-2세로 다시 받아 처리. 푸시 `1a37b98`.

### 2026-06-08
- **버그 수정: 날짜가 "3월 3일"로 리셋되던 현상** — `loadMenuData`의 오늘-주 자동이동이 `wks`(일요일 제외)에서 오늘을 못 찾으면 `wI=0`(데이터 첫 주)에 머물러 항상 데이터 시작 주를 보여줬음. 오늘이 **일요일이거나 데이터 범위 밖**일 때 발생. → 오늘 이후 가장 가까운 주, 없으면(미래) 최신 주로 폴백하도록 수정. `index.html`+`index_server.html` 동기화, 푸시 완료(`3f42cd1`).

### 2026-06-07
- **버그 수정: 동대문 6월 조리법 누락** — `build.py` 수동 JSON 분기가 레시피를 `{}`로 넘겨 동대문 6월 레시피가 0/190이었음. 수동 JSON도 같은 그룹의 레시피 파일을 파싱하도록 수정 → 144/190.
- **매칭 로직 개선**: `match_recipe`를 정규화(괄호 내용·알레르기 원문자·공백 제거) 후 매칭으로 변경. (1) 창원 알레르기기호 레시피명("감자맑은국 ⑤⑥") 정확매칭 → 61%→67%, (2) "토마토→토마토스파게티" 등 짧은 과일·간식명 오매칭 4종 제거, (3) 괄호 대체메뉴("(순두부백탕)")는 폴백으로 보존.
- 데이터 손실 없음(10지역 115키 동일). 푸시 완료.

### 2026-06-04
- 6월 식단 3곳 추가: 광진구·창원시(PDF+레시피 자동 파싱), 동대문구(신규 가로형 그리드 → 좌표 파싱 수동 JSON, 알레르기 24일 검증 후 반영)
- 지역 드롭다운 개선: 이번 달 업데이트 지역 상단 정렬 + 지역명 뒤 ✨ 표시 (네이티브 select라 색배지 불가 → 이모지)
- 데이터 삭제: 대구서구·미소시루·강솔초·의왕시 (임베딩 + 5월 uploads JSON 정리)
- 푸시 완료 (theyirul/meal-planner, 최신 b02c5de)
- 제품 방향 논의 → B안(개인 셀프서브 + AI 비전 파싱) 디벨롭하기로 합의
