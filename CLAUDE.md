# 매일아침 (MealMorning) 프로젝트

## 작업 원칙
1. 실행하라고 명령하는 게 아니라, 고민이라는 판단이 들면 해당 의견에 대해서 답변을 해줘.
2. 중간중간 "Julie Zhuo critique"의 역할로 사용자 입장에서 해당 UX가 어떤지 판단하고 개선 의견을 남겨줘.
3. **구현 전에 반드시 논의부터 하기.** 사용자가 명시적으로 요청함: "나랑 한 번 이야기를 먼저 하고 진행하자."

## 프로젝트 개요
알레르기를 보유한 아기를 키우는 부모가 어린이집 식단을 확인하고, 먹을 수 있는 것/없는 것을 체크해서 어린이집에 공유하는 서비스.

핵심 플로우: 매일 아침 → 오늘 식단 확인 → 알레르기 항목 자동 체크 → "어린이집에 보내기" 복사 → 메신저로 전송

## 기술 구조
- `index_server.html` — 소스 파일 (이것만 편집)
- `build.py` — index_server.html에 JSON 데이터를 임베딩하여 → `index.html` 생성
- `parse_pdf.py` — PDF 식단표 파싱 (pdfplumber, 5행/주 블록 구조)
- `centers.json` — 어린이집 레지스트리
- `tracker.csv` — 8개 어린이집 추적 현황
- 알레르기 표기: `메뉴명(1,2,5,6)` 형식, 식약처 19종 기준

## 현재 UI 구조
- **2탭**: 오늘 / 월간
- **⚙️ 설정**: 전체화면 오버레이 (탭 아님)
- **오늘 탭**: 상세 패널(위) + 날짜 카드(아래). 오늘 자동 선택
- **월간 탭**: 캘린더 + 접힌 검색 기능
- **설정**: 지역→어린이집 캐스케이딩 셀렉트, 알레르기 그리드, 저장/초기화
- **모바일 퍼스트**: :hover 없음, :active만 사용
- 첫 방문 시 설정 오버레이 자동 오픈

## 확정된 디자인 결정
- 인사이트 블록: 개인화된 알레르기 메시지 (안전/경고)
- 체크+복사 워크플로: 알레르기 항목 자동 체크(제외), 복사 버튼
- 오늘 표시: 작은 점(dot), 선택 시 별도 스타일
- 설정 초기화 기능 있음
- 검색은 월간에서 접힌 상태로 제공 (주 기능 아님)

## 미결 사항 (논의 필요)
1. **리퍼럴 링크 복원** — 상품 추천+리퍼럴을 어디에 배치할지 미정 (상세 패널 하단 vs 별도 탭)
2. **레시피 없을 때 UX** — 옵션 C(펼침 화살표 숨기기) 추천했으나 미확정
3. **베타 데이터 확장** — 김포/용인 어린이집 추가. 급식관리지원센터(dietary4u.mfds.go.kr)에서 PDF 다운로드 방식 추천
4. **알레르기 필터 바** — 메인 뷰에서 제거 여부 미정
5. **월간 캘린더 오늘 표시** — 오늘 테두리 vs 알레르기 빨간 테두리 충돌. 날짜 색상으로 구분 제안, 미구현
6. **GitHub 동기화** — theyirul/meal-planner 레포 푸시 보류 중

## 데이터 현황
- active: sample-daycare(광진구), suwon-yudae(수원시)
- source_found: gimpo(김포시), mujigae(용인시) — PDF 확보 가능
- requested: 4곳 (이미지 전용, OCR 어려움)

## 주의사항
- 반드시 `index_server.html`을 편집할 것 (index.html은 build.py가 덮어씀)
- build.py 실행: `python scripts/build.py`
